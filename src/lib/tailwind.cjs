const colors = require("tailwindcss/colors")


module.exports = {
    theme: {
        colors: {
            gray: colors.gray,
            rose: colors.rose,
            red: colors.red,
            orange: colors.orange,
            yellow: colors.amber,
            green: colors.green,
            emerald: colors.emerald,
            blue: colors.blue,
            white: "#fff"
        }
    }
}